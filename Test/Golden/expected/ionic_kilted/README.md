# three_dof_arm_description

Auto-generated by SW2GZ for ROS 2 **Kilted** + Gz **Ionic**.

## Build

```bash
cd ~/ros2_ws/src && cp -r /path/to/this/pkg .
cd ~/ros2_ws && colcon build --packages-select three_dof_arm_description
source install/setup.bash
```

## Launch

```bash
# RViz only (no physics)
ros2 launch three_dof_arm_description display.launch.py

# Gz Sim with ros2_control
ros2 launch three_dof_arm_description gz_sim.launch.py
ros2 launch three_dof_arm_description ros2_control.launch.py
```
